{
  "id": "org.c9.function.biosafety.bua.questionnaire_prompts.v1",
  "name": "bua_questionnaire_prompt_retriever",
  "description": "Retrieves stage-specific conversational instructions for the LLM. The conversation's purpose is to have the user give info about the BUA's information in a structured way, and this function provides the LLM with the appropriate instructions to ask the user for the right information at each stage.",
  "type": "function",
  "keywords": [
    "biosafety",
    "bua",
    "prompt",
    "instructions",
    "routing"
  ],
  "inputs": [
    {
      "name": "stage",
      "type": "string",
      "description": "The current stage of the interview (e.g., 'start', 'project_and_pi', 'additional_contacts', 'personnel', 'room_usage', 'biological_agents', 'project_summary')."
    }
  ],
  "outputs": [
    {
      "type": "object",
      "description": "A dictionary containing the 'prompt_text' with specific stage instructions for the LLM."
    }
  ],
  "examples": [
    {
      "input": {
        "stage": "project_and_pi"
      },
      "output": {
        "status": "success",
        "prompt_text": "You are gathering Project and PI Information. Ask the user for the BUA Number (if known), Project Title, and the PI's Name, Title, Department..."
      }
    }
  ],
  "execution_details": {
    "language": "python",
    "source": "modules/biosafety/tools/questionnaire_prompts.py",
    "initialization": "initiate",
    "execution": "run"
  }
}